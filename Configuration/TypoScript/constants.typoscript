plugin.tx_paypalsubscription {
	persistence {
		storagePid = {$plugin.tx_cart.persistence.storagePid}
	}
	settings {
		sandbox = 1
		client = PAYPAL-CLIENT-ID
		secret = PAYPAL-CLIENT-SECTRET
		subscription {
			pid =
		}
	}
}
